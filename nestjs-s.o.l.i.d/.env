# Prisma 6 resolved a relative sqlite path against the schema directory. Prisma 7
# hands the URL to a driver adapter, which resolves it against the process CWD —
# so this must name the path from the project root, not from prisma/.
DATABASE_URL="file:./prisma/dev.db"
